import 'package:flutter/material.dart';

/// A class that holds a global key for the navigator state.
class NavigatorKeys {
  /// The global key for the navigator state.
  final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
}
